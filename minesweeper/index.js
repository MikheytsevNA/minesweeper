import minesweeperMatrix from './mainClass.js';
import makecanvas from './makeUI.js';

function main() {
  makecanvas();

  /* let matrix = new minesweeperMatrix(5, 5, [2, 1]);
  matrix.fillWithMines(4);
  matrix.fillRestOfField();
  matrix.printAll();
  matrix.makeFlag([2, 0]);
  matrix.makeFlag([2, 2]);
  matrix.openOnFlags([2, 1])
  matrix.checkWin();
  console.log(123);
  matrix.printOpened(); */
}

main();